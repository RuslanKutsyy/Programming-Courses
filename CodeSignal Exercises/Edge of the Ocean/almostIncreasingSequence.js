function almostIncreasingSequence(sequence) {
    let increasing = true;
    for (let i = 0; i < sequence.length; i++){
        if (sequence[ I + 1] <= sequence) {
            increasing = false;
            //sequence[i + 1]
        }
    }
}

almostIncreasingSequence([1, 3, 2, 1]);